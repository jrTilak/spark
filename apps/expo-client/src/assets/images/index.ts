import logoWithName from "./logo-with-name.png";
import onboard from "./onboard.png";
import path from "./path.png";

export const IMAGES = {
  logoWithName,
  onboard,
  path,
};

export type ImageKey = keyof typeof IMAGES;
